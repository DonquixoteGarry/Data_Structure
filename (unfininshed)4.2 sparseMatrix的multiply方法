/*
编写一个方法，把两个储存在一位数组的稀缺矩阵相乘
ps.假定两个矩阵和结果矩阵都是按行主次序存储
*/


#include<iostream>
using namespace std;
template<class T> class elem//矩阵元素elem
{
	int column, line;
	T value;
};
template<class T> class list//存储elem型元素的元素列list
{
	T* list;
};
template<class T> class matrix//矩阵类
{
	int col, lin;
	list<elem<T>> 
	matrix(int column, int line)
	{
		col = column;
		lin  = line;
	}
	void multiply(matrix<T> x, matrix<T> y)//矩阵相乘方法
	{

	}
	void insert(int column, int line, T val)
	{
		 
	}
};
